description: Applies the configured RGB binding for the current RGB mode.

compatible: "zmk,behavior-rgb-mode"

include: zero_param.yaml

properties:
  bindings:
    type: phandle-array
    required: true
    description: Exactly two bindings in the order ON, OFF.