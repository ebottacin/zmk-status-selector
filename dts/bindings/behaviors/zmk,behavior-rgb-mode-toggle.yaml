description: Toggles an RGB mode behavior state.

compatible: "zmk,behavior-rgb-mode-toggle"

include: zero_param.yaml

properties:
  rgb-mode:
    type: phandle
    required: true
    description: RGB mode behavior node controlled by this toggle.